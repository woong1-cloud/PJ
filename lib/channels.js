// 채널은 전사 고정 목록이다. 브랜드마다 다른 값을 쓰면 채널별 집계가
// 불가능해진다 — "채널별로 모아 본다"는 목적 자체가 무너진다. 그래서
// brand_categories 처럼 브랜드별 설정으로 두지 않는다.
//
// 카테고리(무슨 종류의 일인가)와는 축이 다르므로 별도 컬럼이다.
// 0009 마이그레이션의 CHECK 제약과 반드시 같아야 한다.
export const CHANNELS = ['자사몰', '오프라인', '외부몰', '공통', '기타'];

// DB default 와 일치해야 한다(0009).
export const DEFAULT_CHANNEL = '공통';
